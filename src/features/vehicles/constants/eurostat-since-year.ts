/**
 * First calendar year requested from Eurostat for this dataset (electric / hybrid
 * registrations are not meaningfully comparable before this period in the API).
 * Vehicle forms use the same lower bound so local records stay aligned with seeded data.
 */
export const EUROSTAT_SINCE_YEAR = 2018;
