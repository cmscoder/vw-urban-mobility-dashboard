/** Shared Recharts `Tooltip` panel styles for motor-energy charts. */
export const MOTOR_ENERGY_CHART_TOOLTIP_CONTENT_STYLE = {
  borderRadius: '8px',
  border: '1px solid hsl(var(--border))',
  boxShadow: '0 2px 8px rgba(0,0,0,0.08)',
} as const;
